{
  "name": "@utoolios/core",
  "version": "0.0.0",
  "private": true,
  "description": "The stable center: the ToolPlugin contract, shared types, and pure utilities. Depends on NOTHING framework-related (docs/05, docs/13).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@utoolios/config": "workspace:*",
    "typescript": "^5.6.3",
    "vitest": "^2.1.0"
  }
}
