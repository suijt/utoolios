{
  "name": "@utoolios/engine",
  "version": "0.0.0",
  "private": true,
  "description": "The platform brain: routing, metadata, JSON-LD, and sitemap generation from tool plugins. Depends only on core (docs/05, docs/13-16).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@utoolios/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.0"
  }
}
